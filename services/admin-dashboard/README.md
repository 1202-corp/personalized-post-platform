# Admin Dashboard

Панель мониторинга для Personalized Post Bot.

## 🔒 Доступ через SSH туннель

Админ-панель **не доступна публично** из соображений безопасности. Для доступа используйте SSH туннель.

### Быстрый старт

```bash
# Создать туннель (выполнить на локальной машине)
ssh -L 3000:localhost:3000 -L 8000:localhost:8000 -L 5050:localhost:5050 -L 6333:localhost:6333 -L 8080:localhost:8080 root@85.198.98.125
```

После этого откройте в браузере:
- **Admin Dashboard**: http://localhost:3000
- **API Docs**: http://localhost:8000/docs

### Подробная инструкция

1. **Откройте терминал** на вашем компьютере

2. **Создайте SSH туннель**:
   ```bash
   ssh -L 3000:localhost:3000 -L 8000:localhost:8000 -L 5050:localhost:5050 -L 6333:localhost:6333 -L 8080:localhost:8080 root@85.198.98.125
   ```
   
   Параметры:
   - `-L 3000:localhost:3000` — пробрасывает порт 3000 (dashboard)
   - `-L 8000:localhost:8000` — пробрасывает порт 8000 (API)
   - `-L 5050:localhost:5050` — пробрасывает порт 5050 (postgres)
   - `-L 6333:localhost:6333` — пробрасывает порт 6333 (qdrant)
   - `-L 8080:localhost:8080` — пробрасывает порт 8080 (miniapp)
   - `root@85.198.98.125` — подключение к VPS

3. **Введите пароль** от сервера

4. **Откройте браузер** и перейдите на http://localhost:3000

5. **Для завершения** — закройте SSH соединение (Ctrl+C или `exit`)

### Дополнительные порты

| Сервис | Порт | Локальный URL |
|--------|------|---------------|
| Admin Dashboard | 3000 | http://localhost:3000 |
| Core API | 8000 | http://localhost:8000/docs |
| pgAdmin | 5050 | http://localhost:5050 |
| Qdrant UI | 6333 | http://localhost:6333/dashboard |
| MiniApp | 8080 | http://localhost:8080 |

Полный туннель для всех сервисов:
```bash
ssh -L 3000:localhost:3000 \
    -L 8000:localhost:8000 \
    -L 5050:localhost:5050 \
    -L 6333:localhost:6333 \
    -L 8080:localhost:8080 \
    root@85.198.98.125
```

### Альтернатива: фоновый режим

```bash
# Запустить туннель в фоне
ssh -fN -L 3000:localhost:3000 -L 8000:localhost:8000 root@85.198.98.125

# Найти и закрыть туннель
ps aux | grep "ssh -fN"
kill <PID>
```

## 📊 Функции панели

- Статус всех сервисов (health check)
- Статистика пользователей и постов
- A/B тестирование алгоритмов
- Графики активности

## 🔧 Разработка

Для локальной разработки:
```bash
cd services/admin-dashboard
python -m http.server 3000
```
